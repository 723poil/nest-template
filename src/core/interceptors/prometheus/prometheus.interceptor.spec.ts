import { PrometheusInterceptor } from "./prometheus.interceptor";

describe("PrometheusInterceptor", () => {
  it("should be defined", () => {
    expect(new PrometheusInterceptor()).toBeDefined();
  });
});
